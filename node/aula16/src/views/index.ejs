<!-- Usando include para separar as partes que não irão ser modificadas, como o head por exemplo. -->

<%- include('includes/head')%>

<body>
    <section class="container">
        <h1>

            <% if(typeof titulo !== 'undefined') { %>
                <%= titulo %> <!-- escapa os caracteres -->
                <%- titulo %> <!-- não escapa os caracteres (executa) -->

            <% } else{ %>
                    O titulo não foi enviado
                <%}%> 
                <br>

                <% numeros.forEach(num =>{ %>
                    <%= num %>
                <% }) %>

        </h1>
    <form action="" method="post">
        <label>Tecnologia preferida:</label>
        <input type="text" name="tec">
        <button>Enviar</button>
    </form>
    </section>

    <%- include('includes/footer');%>